const Strava={
CID:'TWOJ_CLIENT_ID',URL:'TWOJ_WORKER_URL',
authorize(){location.href='https://www.strava.com/oauth/authorize?client_id='+this.CID+'&response_type=code&redirect_uri='+encodeURIComponent(location.origin+location.pathname)+'&scope=activity:read_all'},
async handleCallback(){const c=new URLSearchParams(location.search).get('code');if(!c)return false;try{const r=await fetch(this.URL,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code:c})});const d=await r.json();if(d.access_token){S.setStravaTokens(d);history.replaceState({},'',location.pathname);return true}}catch(e){}return false},
async syncWorkouts(){const t=S.getStravaTokens();if(!t)return 0;try{const r=await fetch('https://www.strava.com/api/v3/athlete/activities?after='+Math.floor(new Date('2026-06-08')/1000)+'&before='+Math.floor(new Date('2026-09-07')/1000)+'&per_page=100',{headers:{Authorization:'Bearer '+t.access_token}});const acts=await r.json();let n=0;acts.filter(a=>a.type==='Run').forEach(a=>{const d=a.start_date_local.substring(0,10);if(!S.getLog(d).distance){const p=a.moving_time/a.distance*1000,m=Math.floor(p/60),s=Math.round(p%60);S.setLog(d,{distance:(a.distance/1000).toFixed(1),pace:m+':'+String(s).padStart(2,'0'),hr:a.average_heartrate?Math.round(a.average_heartrate):'',status:'done'});n++}});return n}catch(e){return 0}},
isConnected(){const t=S.getStravaTokens();return!!(t&&t.access_token)},
disconnect(){S.setStravaTokens(null)}
};